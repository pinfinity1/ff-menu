services:
  app:
    # 👇 ایمیج را از رجیستری گیت‌هاب تو می‌خواند
    image: ghcr.io/pinfinity1/ff-menu:latest

    # 👇 دستور اجرا ساده شد: فقط آپدیت دیتابیس و استارت
    # (دیگر جنریت و بیلد و سید اینجا انجام نمی‌شود)
    command: sh -c "npx prisma db push && npm start"

    ports:
      - "3000:3000"

    restart: always

    environment:
      NODE_ENV: production
      HOSTNAME: "0.0.0.0"
      PORT: "3000"
      # بقیه متغیرهای محیطی از فایل .env خوانده می‌شوند

  db:
    restart: always

  minio:
    restart: always
    ports:
      - "9000:9000"
